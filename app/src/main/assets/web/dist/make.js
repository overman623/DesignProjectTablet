  function sendMessage(name){
        window.Bridge.setMessage(name);
    }

    function cancelButton(name){
        window.Bridge.cancelButton(name);
    }

    function dialog(name){
        window.Bridge.dialog(name);
    }

    function back(){
       window.history.back();
    }